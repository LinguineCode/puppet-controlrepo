---
:backends:
  - yaml
  - json
:yaml:
  :datadir: /vagrant/hieradata
:json:
  :datadir: /vagrant/hieradata
:hierarchy:
  - "app_tier/%{::app_tier}"
  - "app_role/%{::app_role}"
  - "vagrant/%{::vagrant}"
  - common
